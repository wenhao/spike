spring:
  application:
    name: spring-cloud-eureka-service
eureka:
  instance:
    appname: spring-cloud-eureka-service
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL}
