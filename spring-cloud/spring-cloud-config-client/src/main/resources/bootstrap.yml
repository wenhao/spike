spring:
  application:
    name: spring-config-client
  profiles:
    active: dev
  cloud:
    config:
      uri: "${CONFIG_SERVER_URL}:${CONFIG_SERVER_PORT}"
      username: 'admin'
      password: 'password'