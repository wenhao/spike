server:
  port: 8080
spring:
  application:
    name: spring-config-client
  cloud:
    config:
      url: "${CONFIG_SERVER_URL}:${CONFIG_SERVER_PORT}"
      username: 'admin'
      password: 'password'