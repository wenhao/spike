server:
  port: 8080
spring:
  application:
    name: spring-config-client
  profiles:
    active: "${SPRING_PROFILES_ACTIVE}"
  cloud:
    config:
      uri: "${CONFIG_SERVER_URL}:${CONFIG_SERVER_PORT}"
      username: 'admin'
      password: 'password'
#encrypt:
#  key: devops