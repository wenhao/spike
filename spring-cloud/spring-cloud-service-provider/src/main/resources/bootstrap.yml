spring:
  application:
    name: spring-cloud-service-provider
eureka:
  instance:
    appname: spring-cloud-service-provider
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL}
