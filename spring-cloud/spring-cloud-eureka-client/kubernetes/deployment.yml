apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-cloud-eureka-client
  labels:
    app: spring-cloud-eureka-client
spec:
  selector:
    matchLabels:
      app: spring-cloud-eureka-client
  replicas: 1
  template:
    metadata:
      labels:
        app: spring-cloud-eureka-client
    spec:
      containers:
        - name: eureka-client
          image: wenhao/spring-cloud-eureka-client:1.0.0
          ports:
            - containerPort: 8080
          env:
            - name: EUREKA_URL
              value: "http://spring-cloud-eureka-server:80/eureka/"
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
            - name: CONFIG_SERVER_URL
              value: "http://spring-cloud-config-server-service.microservice"
            - name: CONFIG_SERVER_PORT
              value: "8888"
